{{> header }}

<h2>음식 목록</h2>

{{# userName }}
    <div>{{ userName }}</div>
    <div>{{ userEmail }}</div>
{{/ userName }}

<div>
    <a href="/logout">logout</a>
</div>

<a href="/food/save">Add food</a>


<!-- food table -->
<table>
    <thead>
        <th>id</th>
        <th>name</th>
        <th>start_date</th>
        <th>day_count</th>
    </thead>
    <tbody>
    {{# foodList}}
        <tr>
            <td><a href="/food/{{id}}/reviews">{{ id }}</a></td>
            <td>{{ name }}</td>
            <td>{{ startDate }}</td>
            <td>{{ dayCount }}</td>
            <td><a href="food/update/{{id}}" type="button">수정</a></td>
            <td><button>삭제</button></td>
        </tr>
    {{/foodList}}
    </tbody>

</table>

</html>
